{
  "name": "smoketests",
  "version": "1.0.0",
  "description": "Tests that run against a live set of websocket functions in aws",
  "main": "uploadNewFile.js",
  "scripts": {
    "test": "jasmine test spec/*.js",
    "debug": "node --inspect-brk node_modules/jasmine/bin/jasmine.js",
    "jasmine": "jasmine"
  },
  "devDependencies": {
    "jasmine": "^3.3.1",
    "request": "^2.88.0",
    "websocket": "^1.0.28"
  }
}
